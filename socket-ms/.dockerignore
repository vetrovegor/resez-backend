# Исключаем файлы и директории, которые не нужны в Docker образе
node_modules
dist
uploads
test

# Исключаем конфигурационные файлы
.env
.env.example

# Исключаем файлы конфигурации для сборки и статические анализаторы кода
.eslintrc.js
.prettierrc

# Исключаем файлы логов и временные файлы
npm-debug.log
yarn-debug.log
yarn-error.log

# Исключаем файл README и другие документы
README.md

# Исключаем Dockerfile и файлы Docker Compose, если они не нужны в конечном образе
Dockerfile
docker-compose*.yml

.gitignore
.dockerignore
